This is the home for all my non-sensitive dotfiles. Run bootstrap.sh to setup symlinks and install critical components.

~/.vimrc -> ~/dotfiles/vimrc
~/.vim   -> ~/dotfiles/vim
~/.zshrc -> ~/dotfiles/zshrc

etc
